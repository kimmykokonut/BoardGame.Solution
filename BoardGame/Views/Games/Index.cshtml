@{
  Layout = "_Layout";
}

@using BoardGame.Models;

<h1>List of Board Games</h1>

@if (@Model.Count == 0)
{
  <h3>No games have been added...</h3>
}

@foreach (Game game in Model)
{
  <h4>@Html.ActionLink($"{@game.Name}", "Details", new { id = game.GameId })</h4>
}

<button>@Html.ActionLink("Add new game", "Create")</button>
<button>@Html.ActionLink("Go Home", "Index", "Home")</button>
<button>search for game? (dead)</button>
<p>genres?</p>